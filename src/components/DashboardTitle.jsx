// eslint-disable-next-line react/prop-types
const DashboardTitle = ({ children }) => {
  return (
    <h2 className=" mb-5 text-center text-2xl font-semibold">{children}</h2>
  );
};

export default DashboardTitle;
